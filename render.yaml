services:
  - name: ski-lab-api
    type: web
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn ski_optimizer.api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      # Enter these yourself in the Render dashboard after the first
      # deploy (sync: false means Render won't overwrite/generate them --
      # they must be set manually, and are never stored in this file or
      # in git). Generate SECRET_KEY with:
      #   python -c "import secrets; print(secrets.token_urlsafe(32))"
      - key: SECRET_KEY
        sync: false
      - key: SERPAPI_API_KEY
        sync: false
      - key: DATABASE_URL
        value: sqlite:///./ski_lab.db
      - key: ACCESS_TOKEN_EXPIRE_MINUTES
        value: "15"
      - key: REFRESH_TOKEN_EXPIRE_DAYS
        value: "30"
      - key: EMAIL_BACKEND
        value: console
      # Used for CORS allow-listing (see api/main.py) -- must exactly
      # match ski-lab-app's actual URL below, scheme included.
      - key: FRONTEND_URL
        value: https://ski-lab-app.onrender.com

  - name: ski-lab-app
    type: web
    runtime: static
    plan: free
    staticPublishPath: frontend/app
    buildCommand: ""
